# 5A 完整剧情试玩版 v1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use $superpower-subagents (recommended) or $superpower-executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking via update_plan.

**Goal:** 将 5A 宿舍修补线从灰盒垂直切片升级为完整剧情试玩版 v1，满足 20+ 有效选择和选择前后剧情承接。

**Architecture:** 保留现有灰盒入口，新建 `5a-full-data.js` 与 `5a-full-playtest.html`。升级 `app.js` 支持选择前铺垫、选择后反馈和“继续推进”阅读节奏。用 Node 测试验证数据密度、UI smoke 和核心 engine。

**Tech Stack:** Plain HTML/CSS/JavaScript, CommonJS/UMD, bundled Node.js, PowerShell, `rg`.

---

### Task 1: 写完整数据质量测试

**Files:**
- Create: `playtest/5a-full-data.test.js`

Steps:

- [ ] 创建测试，先要求 `5a-full-data.js` 存在。
- [ ] 校验 `chain.length >= 22`。
- [ ] 校验有选项的节点数 `>= 20`。
- [ ] 校验总选项池 `>= 40`。
- [ ] 校验每个有选项节点都有不少于 80 个字符的 `选择前铺垫`。
- [ ] 校验每个选项都有不少于 80 个字符的 `选择后反馈`。
- [ ] 校验 `娱乐性互动段 >= 4`、`低操作高沉浸段 >= 3`、`关键冲突节点 >= 2`。
- [ ] 运行测试，确认红灯：缺少 `5a-full-data.js`。

Run:

```powershell
& "C:\Users\qingguang\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe" "playtest\5a-full-data.test.js"
```

Expected: fails because `5a-full-data.js` does not exist.

---

### Task 2: 升级通用试玩 UI

**Files:**
- Modify: `playtest/5a-playtest.html`
- Modify: `playtest/app.js`
- Modify: `playtest/styles.css`
- Modify: `playtest/app.smoke.test.js`

Steps:

- [ ] 在 HTML 中新增 `feedback-panel`、`feedback-text`、`continue-button`。
- [ ] 在 `app.js` 中渲染 `过场剧情`、`玩家可见摘要`、`选择前铺垫`。
- [ ] 在 `app.js` 中加入 pending feedback：点击选项后先显示 `选择后反馈`，点击“继续推进”后进入下一节点。
- [ ] 更新 smoke test：第一次点击后应显示 feedback，继续后再进入下一节点。
- [ ] 运行 `app.smoke.test.js`，确认通过。

---

### Task 3: 写入 5A 完整数据 v1

**Files:**
- Create: `playtest/5a-full-data.js`
- Create: `playtest/5a-full-playtest.html`

Steps:

- [ ] 新建 `5a-full-data.js`，导出 23 个节点。
- [ ] 节点链包含入口、冷场、旧账、陆沉、规则会、食堂、期末复发、输出。
- [ ] 每个有选项节点写入 `选择前铺垫`。
- [ ] 每个选项写入 `选择后反馈`。
- [ ] 至少 4 个节点标记 `娱乐性互动段: true`。
- [ ] 至少 3 个节点标记 `低操作高沉浸段: true`。
- [ ] 至少 2 个节点标记 `关键冲突节点: true`。
- [ ] 新建 `5a-full-playtest.html`，引用 `5a-full-data.js`、`engine.js`、`app.js`。
- [ ] 运行 `5a-full-data.test.js`，确认通过。

---

### Task 4: 总体验证和记录

**Files:**
- Modify: `task_plan.md`
- Modify: `findings.md`
- Modify: `progress.md`

Steps:

- [ ] 运行核心逻辑测试。
- [ ] 运行完整数据质量测试。
- [ ] 运行 UI smoke test。
- [ ] 用 `rg` 验证完整入口文件包含挂载点和 `5a-full-data.js`。
- [ ] 更新 planning-with-files 三份记录。

Commands:

```powershell
& "C:\Users\qingguang\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe" "playtest\engine.test.js"
& "C:\Users\qingguang\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe" "playtest\5a-full-data.test.js"
& "C:\Users\qingguang\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe" "playtest\app.smoke.test.js"
rg -n "5a-full-data.js|feedback-panel|continue-button|choice-list|state-list|log-list" "playtest\5a-full-playtest.html" "playtest\5a-playtest.html"
```

Expected: tests pass, rg finds required IDs and script reference.
