# 5A 微选择扩容 v2.1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use $superpower-executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking via update_plan.

**Goal:** 增加 5A 中不改变主线、只影响心态和人物关系的微选择，让试玩手感不再像被强制推主轴。

**Architecture:** 继续使用 `playtest/5a-full-data.js` 的静态数据结构。心理级选项用 `opt(..., undefined, { 心理级选项: true })` 或不传 `解锁` 的方式自然进入下一 chain 节点，不新增路线节点。

**Tech Stack:** Plain JavaScript data module, Node.js tests, static HTML playtest.

---

### Task 1: Add Failing Micro-Choice Tests

**Files:**
- Modify: `playtest/5a-full-data.test.js`

- [ ] **Step 1: Add v2.1 title check**

Assert `data.title` contains `v2.1`.

- [ ] **Step 2: Add micro-choice count checks**

Compute all options where `option.心理级选项 === true` and assert count is at least 10.

- [ ] **Step 3: Add record-scroll checks**

Assert `ACT5_5A05_RECORD_SCROLL` has at least 4 options and includes option text matching:

```js
/关掉手机/
/晚风消息|晚风/
```

- [ ] **Step 4: Add safety checks**

Assert every psychological option:

- does not set `外流点`
- does not unlock old route keys
- if it has `解锁`, it must not point to `ACT5_5X01_STAIRWELL_MUTE` or non-5A/5X nodes

- [ ] **Step 5: Run RED**

Run:

```powershell
node playtest/5a-full-data.test.js
```

Expected: fails because v2.1 micro-choice metadata and record-scroll options do not exist yet.

### Task 2: Implement Micro Choices

**Files:**
- Modify: `playtest/5a-full-data.js`
- Modify: `playtest/5a-full-playtest.html`

- [ ] **Step 1: Update title**

Set both data and HTML title to `第五幕 5A 宿舍修补线正式可玩版 v2.1 微选择扩容`.

- [ ] **Step 2: Add record-scroll options**

Add:

- `关掉手机，先去阳台透口气`
- `晚风消息弹出来，先点开看一眼`

Both must be `心理级选项: true` and must not unlock 5C.

- [ ] **Step 3: Add additional micro choices**

Add at least 8 more `心理级选项: true` options across early and middle 5A nodes.

### Task 3: Sync Docs and Verify

**Files:**
- Modify: `1-6幕剧情优化稿.md`
- Modify: `沉浸式剧情互动系统_v2.json`
- Modify: `task_plan.md`
- Modify: `findings.md`
- Modify: `progress.md`

- [ ] **Step 1: Document micro-choice rule**

Record that psychological choices add playability without route branching.

- [ ] **Step 2: Run verification**

Run:

```powershell
node playtest/5a-full-data.test.js
node playtest/app.smoke.test.js
node playtest/engine.test.js
node --check playtest/5a-full-data.js
node --check playtest/app.js
node -e "const fs=require('fs'); JSON.parse(fs.readFileSync('沉浸式剧情互动系统_v2.json','utf8')); console.log('json ok');"
```
