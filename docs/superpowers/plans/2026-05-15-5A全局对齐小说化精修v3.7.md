# 5A 全局对齐小说化精修 v3.7 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use $superpower-subagents (recommended) or $superpower-executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking via update_plan.

**Goal:** 用 `chinese-novelist` 的小说化重排结果精修 5A playable 数据，让它服从 1-7 全局大纲。

**Architecture:** 先写验收测试，再改数据文本和少量结构字段，最后同步文档与 JSON 记录。

**Tech Stack:** Node/CommonJS 测试、静态 HTML 试玩器、Markdown/JSON 文档。

---

### Task 1: 写 v3.7 验收测试

**Files:**
- Modify: `playtest/5a-full-data.test.js`

- [ ] **Step 1: 增加标题和小说化重排验收**

检查 title 包含 `v3.7`。

- [ ] **Step 2: 增加节点承重验收**

检查：

```js
ACT5_FULL_00_AUDIT: 宿舍群、生活费、父亲、母亲、晚风、抖音、共享文档、活动证明
ACT5_5A05_RECORD_SCROLL: 手机袋、水费、热水卡、外放、空调、陆沉、没事
ACT5_5A15_DOOR_SLAM_RISK: 同质收束入口、唯一硬外流
ACT5_5A16_CANTEEN_SEAT: 筷子、排队、空椅子、手机
ACT5_5A20_RELAPSE_CHOICE: 第六幕入口候选
ACT5_5X99_OUTFLOW_OUTPUT: 第六幕与终章仅走外流单线
```

- [ ] **Step 3: 运行红灯**

Run:

```powershell
node playtest/5a-full-data.test.js
```

Expected: fails because v3.7 title and new text/fields are not implemented yet.

### Task 2: 修改 5A 数据

**Files:**
- Modify: `playtest/5a-full-data.js`

- [ ] **Step 1: 升级 title**
- [ ] **Step 2: 改写 `ACT5_FULL_00_AUDIT` 和 `ACT5_FULL_01_ROUTER`**
- [ ] **Step 3: 改写 `ACT5_5A05_RECORD_SCROLL`**
- [ ] **Step 4: 给 `ACT5_5A15_DOOR_SLAM_RISK` 增加同质收束选项**
- [ ] **Step 5: 改写 `ACT5_5A16_CANTEEN_SEAT` 和 `ACT5_5A20_RELAPSE_CHOICE`**
- [ ] **Step 6: 改写 5X 外流跨幕字段和元叙事句**

### Task 3: 同步外围文档

**Files:**
- Modify: `playtest/5a-full-playtest.html`
- Modify: `docs/5A节点验收表_v2.md`
- Modify: `1-6幕剧情优化稿.md`
- Modify: `沉浸式剧情互动系统_v2.json`
- Modify: `findings.md`
- Modify: `progress.md`

- [ ] **Step 1: 同步标题和 v3.7 说明**
- [ ] **Step 2: 记录 `chinese-novelist` 已用于 5A 小说化重排**

### Task 4: Fresh verification

Run:

```powershell
node playtest/5a-full-data.test.js
node playtest/app.smoke.test.js
node playtest/engine.test.js
node --check playtest/5a-full-data.js
node --check playtest/app.js
node --check playtest/5a-data.js
node -e "JSON.parse(require('fs').readFileSync('沉浸式剧情互动系统_v2.json','utf8')); console.log('json ok')"
node -e "JSON.parse(require('fs').readFileSync('interactive_system_v2.json','utf8')); console.log('legacy json ok')"
```

Expected: all commands exit 0.
