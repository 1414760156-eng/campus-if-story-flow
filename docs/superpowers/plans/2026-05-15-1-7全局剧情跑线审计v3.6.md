# 1-7 全局剧情跑线审计 v3.6 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use $superpower-subagents (recommended) or $superpower-executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking via update_plan.

**Goal:** 先跑通 1-7 全局剧情总轴，再反推 5A 宿舍修补线的改写边界，避免 5A 局部漂移。

**Architecture:** 本轮只写文档，不改 playable 数据。输出分三层：design spec 固定目标，implementation plan 固定执行步骤，审计总文档作为后续改 5A 的总控依据。

**Tech Stack:** Markdown 文档、PowerShell/rg 文件验证、现有 planning-with-files 记录。

---

### Task 1: 整理全局输入证据

**Files:**
- Read: `1-6幕剧情优化稿.md`
- Read: `optimized_story_acts1_6.md`
- Read: `extracted_sources/武生院沉浸版_第三幕纯剧情稿_去剧透版.txt`
- Read: `extracted_sources/武生院沉浸版_第四幕纯剧情稿_二次修订版.txt`
- Read: `extracted_sources/5C_纯剧情稿.txt`
- Read: `extracted_sources/第六幕_剧情总稿_准结局形成阶段_时间流逝正文修正版.txt`
- Read: `extracted_sources/第六幕_Codex接入逻辑总表_时间流逝正文修正版.txt`
- Read: `extracted_sources/第六幕_考研考证方向回调修正补丁.txt`
- Read: `extracted_finale_latest/终章_最后一个新年_Codex总控交付文档.txt`
- Read: `playtest/5a-full-data.js`

- [x] **Step 1: 搜索关键主线锁点**

Run:

```powershell
rg -n --glob '*.txt' "序幕|第一幕|第二幕|第三幕|第四幕|第五幕|第六幕|终章|最后一个新年|军训|手机|汇演|班会|主持|招新|毕业|生活费|晚风|夏知微|沈嘉禾|秦越|许澈" extracted_sources
```

Expected: 能看到 5C、第六幕、终章、军训汇演等关键锚点。

- [x] **Step 2: 读取第六幕和终章总控**

Run:

```powershell
Get-Content -LiteralPath 'extracted_sources\第六幕_剧情总稿_准结局形成阶段_时间流逝正文修正版.txt' -Encoding UTF8 -TotalCount 260
Get-Content -LiteralPath 'extracted_finale_latest\终章_最后一个新年_Codex总控交付文档.txt' -Encoding UTF8 -TotalCount 260
```

Expected: 确认第六幕是准结局形成，终章只做软变量和后日谈。

### Task 2: 写 1-7 全局跑线审计

**Files:**
- Create: `docs/1-7全局剧情跑线审计_v3.6.md`

- [ ] **Step 1: 写总轴结论**

Include these fixed conclusions:

```markdown
- 第一幕到第四幕负责种子、分流和压力升级。
- 第五幕负责把多线压力拆成清晰单线处理。
- 第六幕负责把第五幕结果转成准结局。
- 终章只调整抵达状态，不推翻第六幕硬结局。
```

- [ ] **Step 2: 写幕别功能表**

Cover:

```markdown
序幕/第一幕、第二幕、第三幕、第四幕、第五幕、第六幕、终章
```

Each row must contain:

```markdown
不可改动事件 | 叙事功能 | 已埋人物/暗线 | 对 5A 的约束
```

- [ ] **Step 3: 写人物弧光表**

Cover:

```markdown
林亦舟、周屿、唐骁、陆沉、父母、晚风、沈嘉禾、夏知微、秦越、陈老师、许澈
```

For each, include:

```markdown
早期种子 | 中期冲突 | 第五幕职责 | 第六/终章回响 | 5A 可用边界
```

- [ ] **Step 4: 写 5A 改写边界矩阵**

Must include these nodes:

```markdown
ACT5_FULL_00_AUDIT
ACT5_5A01_CORRIDOR_RETURN
ACT5_5A05_RECORD_SCROLL
ACT5_5A11_TABLE_TIMER
ACT5_5A15_DOOR_SLAM_RISK
ACT5_5A16_CANTEEN_SEAT
ACT5_5A19_EXAM_NIGHT_RETURN
ACT5_5A20_RELAPSE_CHOICE
ACT5_5X01_STAIRWELL_MUTE
ACT5_5X99_OUTFLOW_OUTPUT
```

### Task 3: 更新 planning-with-files

**Files:**
- Modify: `findings.md`
- Modify: `progress.md`
- Modify: `task_plan.md`

- [ ] **Step 1: 在 findings.md 追加本轮发现**

Include:

```markdown
## 2026-05-15：1-7 全局跑线审计发现
```

Must mention:

```markdown
终章不能被遗漏；第六幕已形成准结局；第五幕不是晚风核心；5A 只处理宿舍线；第六幕 9 主预结局以补丁/终章为准。
```

- [ ] **Step 2: 在 progress.md 追加本轮进度**

Include skills used:

```markdown
superpower-bootstrap
superpower-brainstorming
planning-with-files
superpower-writing-plans
chinese-novelist
superpower-verification
```

- [ ] **Step 3: 在 task_plan.md 记录当前重点切换**

Change or append current focus:

```markdown
当前重点从 5A 单点润色切换为 1-7 全局跑线审计，再反推 5A。
```

### Task 4: 验证文档

**Files:**
- Verify: `docs/1-7全局剧情跑线审计_v3.6.md`
- Verify: `docs/superpowers/specs/2026-05-15-1-7全局剧情跑线审计v3.6-design.md`
- Verify: `docs/superpowers/plans/2026-05-15-1-7全局剧情跑线审计v3.6.md`

- [ ] **Step 1: 路径存在验证**

Run:

```powershell
Test-Path 'docs\1-7全局剧情跑线审计_v3.6.md'
Test-Path 'docs\superpowers\specs\2026-05-15-1-7全局剧情跑线审计v3.6-design.md'
Test-Path 'docs\superpowers\plans\2026-05-15-1-7全局剧情跑线审计v3.6.md'
```

Expected: all `True`.

- [ ] **Step 2: 关键词验证**

Run:

```powershell
rg -n "终章|最后一个新年|第六幕|准结局|5A|5X|唯一硬外流|晚风|父母|沈嘉禾|夏知微|秦越|陈老师|许澈|9 个主预结局" docs\1-7全局剧情跑线审计_v3.6.md
```

Expected: all key topics have hits.

- [ ] **Step 3: 玩家可见文风禁令验证**

Run:

```powershell
rg -n "下一句话会把|这一步会被记进|周屿怕|唐骁怕|陆沉怕|让林亦舟意识到" docs\1-7全局剧情跑线审计_v3.6.md
```

Expected: no matches.
