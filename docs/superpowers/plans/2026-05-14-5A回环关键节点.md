# 5A 回环关键节点 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use $superpower-executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking via update_plan.

**Goal:** 在 5A 完整试玩版中加入“走廊不进门”的短支线，并建立关键节点可短暂绕行后回到寝室主轴的规则。

**Architecture:** 继续使用现有静态 HTML + `5a-full-data.js` + `engine.js`。不改引擎，利用现有 `选项.解锁` 字段实现支线跳转；支线节点插入 `chain` 中，并由支线选项自然推进回主轴。

**Tech Stack:** Plain JavaScript data module, Node.js tests, static HTML playtest.

---

### Task 1: Add Failing Route-Loop Test

**Files:**
- Modify: `playtest/5a-full-data.test.js`

- [ ] **Step 1: Add assertions for corridor loop behavior**

Add checks after the existing conflict-node validation:

```js
const corridorNode = nodes.ACT5_5A01_CORRIDOR_RETURN;
const outsideNode = nodes.ACT5_5A01A_OUTSIDE_BENCH;
assert.ok(corridorNode, "corridor return node should exist");
assert.ok(outsideNode, "outside bench loop node should exist");
const outsideOption = (corridorNode.可选互动 || []).find((option) => /不进门|楼外|长椅/.test(option.选项));
assert.ok(outsideOption, "corridor node should offer a non-entry route option");
assert.deepStrictEqual(outsideOption.解锁, ["ACT5_5A01A_OUTSIDE_BENCH"], "non-entry route should unlock outside bench loop");
assert.strictEqual(outsideNode.回环节点, true, "outside bench should be marked as a loop node");
assert.strictEqual(outsideNode.回流节点, "ACT5_5A02_DOORWAY_PAUSE", "outside bench should return to doorway pause");
(corridorNode.可选互动 || [])
  .filter((option) => option !== outsideOption)
  .forEach((option) => {
    assert.deepStrictEqual(option.解锁, ["ACT5_5A02_DOORWAY_PAUSE"], `${option.选项} should go directly to doorway pause`);
  });
```

- [ ] **Step 2: Run test and confirm RED**

Run:

```powershell
node playtest/5a-full-data.test.js
```

Expected: fail because `ACT5_5A01A_OUTSIDE_BENCH` does not exist yet.

### Task 2: Add Corridor Non-Entry Loop Node

**Files:**
- Modify: `playtest/5a-full-data.js`

- [ ] **Step 1: Insert loop node in chain**

Insert `ACT5_5A01A_OUTSIDE_BENCH` between `ACT5_5A01_CORRIDOR_RETURN` and `ACT5_5A02_DOORWAY_PAUSE`.

- [ ] **Step 2: Update corridor options**

Set the two existing enter-room options to unlock `ACT5_5A02_DOORWAY_PAUSE`.
Add a third option:

```js
opt("不进门，去寝室楼外坐一会儿", "...", { 回避值: 2, 晚风牵引: 2, 现实压力: -1 }, ["ACT5_5A01A_OUTSIDE_BENCH"])
```

- [ ] **Step 3: Create loop node**

Create `ACT5_5A01A_OUTSIDE_BENCH` with:

- 场景：寝室楼外 / 长椅 / 晚风聊天入口
- 至少两个选项：回晚风消息、碰见买夜宵的室友
- `回环节点: true`
- `回流节点: "ACT5_5A02_DOORWAY_PAUSE"`
- `低操作高沉浸段: true`

Each option can rely on normal chain progression to return to `ACT5_5A02_DOORWAY_PAUSE`.

### Task 3: Verify and Record

**Files:**
- Modify: `findings.md`
- Modify: `progress.md`
- Modify: `task_plan.md`

- [ ] **Step 1: Run verification**

Run:

```powershell
node playtest/5a-full-data.test.js
node playtest/app.smoke.test.js
node playtest/engine.test.js
node --check playtest/5a-full-data.js
node --check playtest/app.js
```

Expected:

- `5A full data tests passed`
- `app smoke test passed`
- `engine tests passed`
- syntax checks exit `0`

- [ ] **Step 2: Record the rule**

Add a finding that route-level choices should use short loop nodes, while psychology-only choices should modify state without route jumps.

## Verification

The change is complete only if the full data test proves:

- non-entry option exists,
- it unlocks the outside loop node,
- the loop node returns to `ACT5_5A02_DOORWAY_PAUSE`,
- direct entry options skip the loop,
- complete playthrough still reaches output page.

## Next skill

`$superpower-executing-plans`
