# 5A 关键路径试玩阅读精修 v3.8 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use $superpower-subagents (recommended) or $superpower-executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking via update_plan.

**Goal:** Clean the remaining non-immersive wording in 5A key playable paths while preserving the existing structure.

**Architecture:** Keep `playtest/5a-full-data.js` as the single source for playable story text and `playtest/5a-full-data.test.js` as the regression gate. Sync version labels and delivery docs after the data passes.

**Tech Stack:** Node.js CommonJS data module, Node assert tests, JSON delivery files, Markdown planning docs.

---

### Task 1: Add Text-Quality Regression Tests

**Files:**
- Modify: `playtest/5a-full-data.test.js`

- [ ] **Step 1: Add v3.8 title assertion**

Add:

```js
assert.ok(data.title.includes("v3.8"), "title should mark key-path reading polish v3.8");
```

- [ ] **Step 2: Add forbidden player-facing patterns**

Add these patterns to the existing forbidden list:

```js
/突然明白/,
/帮人发声/,
/按钮/,
/北园便利店/,
/南门那边/,
/第六幕会从/,
/读取他们的底盘/,
```

- [ ] **Step 3: Run red test**

Run:

```powershell
node playtest/5a-full-data.test.js
```

Expected: FAIL on missing v3.8 or the newly forbidden phrases.

### Task 2: Rewrite Key Path Text

**Files:**
- Modify: `playtest/5a-full-data.js`

- [ ] **Step 1: Upgrade title to v3.8**

Append `-v3.8` and describe it as `关键路径试玩阅读精修`.

- [ ] **Step 2: Replace abstract cognition**

Rewrite:

- `林亦舟突然明白沉默也会变成旧账`
- `帮人发声如果抢了语气，也会把边界推到别处`
- `每个按钮前后都有人被影响`

Each replacement must use visible objects or dialogue, not explanatory cognition.

- [ ] **Step 3: Correct map wording**

Replace:

- `北园便利店` with `西园餐厅侧门便利店`
- `南门那边` with `东区商业街另一头`

- [ ] **Step 4: Move 第六幕说明 out of player-facing setup**

Remove the direct line beginning with `第六幕会从这种复发里读取...` from `ACT5_5A20_RELAPSE_CHOICE` setup. Keep the structured extra field `第六幕入口候选` unchanged.

- [ ] **Step 5: Run green test**

Run:

```powershell
node playtest/5a-full-data.test.js
```

Expected: `5A full data tests passed`.

### Task 3: Sync Delivery Files And Verify

**Files:**
- Modify: `playtest/5a-full-playtest.html`
- Modify: `docs/5A节点验收表_v2.md`
- Modify: `1-6幕剧情优化稿.md`
- Modify: `沉浸式剧情互动系统_v2.json`
- Modify: `progress.md`

- [ ] **Step 1: Sync v3.8 version labels and rules**

Record that v3.8 is a key-path reading polish pass and does not add nodes.

- [ ] **Step 2: Run full verification**

Run:

```powershell
node playtest/5a-full-data.test.js
node playtest/app.smoke.test.js
node playtest/engine.test.js
node --check playtest/5a-full-data.js
node --check playtest/app.js
node --check playtest/5a-data.js
node -e "JSON.parse(require('fs').readFileSync('沉浸式剧情互动系统_v2.json','utf8')); JSON.parse(require('fs').readFileSync('interactive_system_v2.json','utf8')); console.log('json ok')"
```

Expected: all commands exit 0 and the test commands print their pass messages.
