# 5A 多边缘人物回流 v3.3 执行计划

## 目标

把食堂侧门和 5X 外流后段升级为多边缘人物参与结构：食堂侧门可遇见夏知微、沈嘉禾、秦越；5X 中许澈仍是新人核心，但秦越可作为已有边缘人物擦肩出现。所有新增内容只影响微心态和软关系，不解锁旧主线。

## 步骤

1. 更新测试
   - 标题断言升级到 `v3.3`。
   - 节点数、选项数、心理级微选择、回环节点、微心态聊天节点阈值升级。
   - 校验 `ACT5_5A16_CANTEEN_SEAT` 至少 4 种回流方式。
   - 校验食堂侧门沈嘉禾、秦越路线存在，并拥有“聊刚发生的事/不聊宿舍”的选项。
   - 校验 5X 秦越软擦肩节点存在，不回流、不解锁 5F。

2. 更新 `playtest/5a-full-data.js`
   - 标题升级到 v3.3。
   - 在 5A16 源点新增沈嘉禾、秦越两条食堂侧门路线。
   - 新增沈嘉禾路线节点：入口、聊饭桌现实、不聊宿舍只聊复习。
   - 新增秦越路线节点：入口、聊饭桌现实、不聊宿舍只聊活动。
   - 在 5X 许澈未来节点后加入秦越软擦肩节点。

3. 同步文档
   - 更新试玩 HTML 标题。
   - 更新 `docs/5A节点验收表_v2.md`。
   - 更新 `1-6幕剧情优化稿.md`。
   - 更新 `沉浸式剧情互动系统_v2.json`。

4. 验证
   - 运行完整数据测试、app smoke、engine 测试。
   - 运行 JS 语法检查和 JSON 解析。
   - 输出统计确认。

5. 记录
   - 更新 `task_plan.md`、`findings.md`、`progress.md`。
