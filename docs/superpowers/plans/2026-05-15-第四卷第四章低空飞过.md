# 第四卷第四章低空飞过 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use $superpower-subagents (recommended) or $superpower-executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking via update_plan.

**Goal:** 续写第四卷第四章《低空飞过》，把成绩查询、补考压力、寝室桌面物件和家庭/晚风消息写成完整小说章节。

**Architecture:** 本任务是文档型创作，不改互动试玩器代码。先追加正文，再更新分卷目录与进度文件，最后用 rg 和字数统计校验正文禁用表达、明德楼地点逻辑和新增锚点。

**Tech Stack:** Markdown 正文；PowerShell + rg 校验；Codex apply_patch 编辑。

---

### Task 1: 追加第四卷第四章正文

**Files:**
- Modify: `docs/剧情小说母版_v4.0/正文_第四卷_夏天没有停下来.md`

- [ ] **Step 1: 在第三章末尾后追加 `## 第四章 低空飞过`**

内容必须包含：

```text
成绩加载完成；林亦舟数据结构入门分数落在补考线附近或补考线内。
周屿、唐骁、陆沉以不同方式回应。
青枫居四张桌子、插排、空调遥控器、旧耳机和公共桌参与叙事。
母亲成绩询问、父亲转账备注、晚风微信陪伴同时压来。
补考安排或资料消息在章末形成下一章钩子。
```

- [ ] **Step 2: 自检正文用词**

追加后检查本章不出现：

```text
节点、变量、触发、回流、系统提示、意识到、忽然、突然、发现、觉得、害怕、主线、副线、分线、flag、权重、判定、资源线、NPC
```

### Task 2: 更新目录与进度

**Files:**
- Modify: `docs/剧情小说母版_v4.0/01_分卷目录与40万字进度表.md`
- Modify: `progress.md`
- Modify: `task_plan.md`

- [ ] **Step 1: 将第四卷状态改为第四章完成**

把目录表中第四卷状态更新为：

```text
已开卷，第四章完成
```

- [ ] **Step 2: 在当前累计进度表追加一行**

记录第四章新增内容、正文合计、第四卷字数和总章数。

- [ ] **Step 3: 在 progress.md 追加本轮记录**

记录使用技能、文件变更、章节推进、地点逻辑、校验结果。

- [ ] **Step 4: 在 task_plan.md 当前重点里追加本轮完成项**

记录第四卷第四章完成，并把下一步指向第四卷第五章。

### Task 3: 校验

**Files:**
- Check: `docs/剧情小说母版_v4.0/正文_*.md`

- [ ] **Step 1: 正文禁用表达扫描**

Run:

```powershell
rg -n -g "正文_*.md" "节点|变量|触发|回流|系统提示|下一句话会|意识到|让林亦舟意识到|周屿怕|唐骁怕|陆沉怕|这一步会被记|忽然|突然|发现|觉得|害怕|主线|副线|分线|flag|权重|判定|回主线|资源线|NPC" docs\剧情小说母版_v4.0
```

Expected: no matches.

- [ ] **Step 2: 明德楼误用扫描**

Run:

```powershell
rg -n -g "正文_*.md" "明德楼.*(班会|专业导论|咨询会|试训|上机|机房)|班会.*明德楼|专业导论.*明德楼|咨询会.*明德楼|试训.*明德楼|上机.*明德楼|机房.*明德楼" docs\剧情小说母版_v4.0
```

Expected: no matches.

- [ ] **Step 3: 锚点扫描**

Run:

```powershell
rg -n "第四章 低空飞过|数据结构入门|补考|教务系统|公共桌|插排|空调遥控器|旧耳机|周屿|唐骁|陆沉|晚风" docs\剧情小说母版_v4.0\正文_第四卷_夏天没有停下来.md
```

Expected: matches in new chapter.

- [ ] **Step 4: 字数统计**

Run:

```powershell
$root='docs\剧情小说母版_v4.0'
$body=Get-ChildItem -LiteralPath $root -Filter '正文_*.md' | Sort-Object Name
$body | ForEach-Object { '{0}: {1}' -f $_.Name, (Get-Content -LiteralPath $_.FullName -Raw -Encoding UTF8).Length }
'BODY_TOTAL {0}' -f (($body | ForEach-Object { (Get-Content -LiteralPath $_.FullName -Raw -Encoding UTF8).Length } | Measure-Object -Sum).Sum)
```

Expected: 正文合计大于上一轮 184,578 字符，第四卷字数增加。
