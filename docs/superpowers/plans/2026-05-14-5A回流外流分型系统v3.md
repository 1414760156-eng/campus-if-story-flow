# 5A 回流/外流分型系统 v3 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use $superpower-subagents (recommended) or $superpower-executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking via update_plan.

**Goal:** Upgrade the 5A playable story data from simple extended loops to typed homogeneous/heterogeneous return and terminal convergence/outflow behavior.

**Architecture:** Keep the existing static data format and engine. Add explicit metadata fields to nodes/options and add new nodes only where needed for a corridor vertical slice and a 5A-specific convergence output.

**Tech Stack:** Plain JavaScript data module, Node.js assertion tests, static HTML playtest page.

---

### Task 1: Add Failing v3 Data Tests

**Files:**
- Modify: `playtest/5a-full-data.test.js`

- [ ] **Step 1: Add v3 title and typed-flow assertions**

Add assertions after existing v2/v2.1/v2.2 title checks:

```js
assert.ok(data.title.includes("v3"), "title should mark typed return/outflow v3");
```

Add typed-flow collections after `const conflictNodes`:

```js
const homogeneousReturnNodes = allNodes.filter((node) => node.流向类型 === "同质回流");
const heterogeneousSoftReturnNodes = allNodes.filter((node) => node.流向类型 === "异质软回流");
const homogeneousConvergenceNodes = allNodes.filter((node) => node.流向类型 === "同质收束");
const heterogeneousOutflowNodes = allNodes.filter((node) => node.流向类型 === "异质外流");
```

Add assertions:

```js
assert.ok(homogeneousReturnNodes.length >= 4, `expected 4+ homogeneous return nodes, got ${homogeneousReturnNodes.length}`);
assert.ok(heterogeneousSoftReturnNodes.length >= 2, `expected 2+ heterogeneous soft return nodes, got ${heterogeneousSoftReturnNodes.length}`);
assert.ok(homogeneousConvergenceNodes.length >= 1, "expected a 5A homogeneous convergence node");
assert.ok(heterogeneousOutflowNodes.length >= 4, `expected 4+ heterogeneous outflow nodes, got ${heterogeneousOutflowNodes.length}`);
```

- [ ] **Step 2: Add corridor split assertions**

Assert the corridor node has both route types:

```js
const lakeOption = (corridorNode.可选互动 || []).find((option) => /湖边|绕一圈/.test(option.选项));
assert.ok(lakeOption, "corridor node should offer a lake-side heterogeneous soft return option");
assert.deepStrictEqual(lakeOption.解锁, ["ACT5_5A01C_LAKESIDE_SOFT_RETURN"], "lake option should unlock lakeside soft return");
assert.strictEqual(nodes.ACT5_5A01C_LAKESIDE_SOFT_RETURN.流向类型, "异质软回流", "lakeside node should be heterogeneous soft return");
assert.strictEqual(nodes.ACT5_5A01C_LAKESIDE_SOFT_RETURN.禁止解锁分线, true, "lakeside soft return should forbid route unlocking");
assert.strictEqual(nodes.ACT5_5A01D_LAKESIDE_BACK.回流节点, "ACT5_5A02_DOORWAY_PAUSE", "lakeside soft return should return to doorway pause");
```

- [ ] **Step 3: Add terminal convergence assertions**

Assert the unique terminal node can go to 5A convergence as well as 5X:

```js
const convergenceOption = nodes.ACT5_5A15_DOOR_SLAM_RISK.可选互动.find((option) => option.流向类型 === "同质收束");
assert.ok(convergenceOption, "door slam risk should offer a 5A homogeneous convergence option");
assert.deepStrictEqual(convergenceOption.解锁, ["ACT5_5A15B_DORM_CONVERGENCE"], "homogeneous convergence should unlock 5A convergence node");
assert.strictEqual(nodes.ACT5_5A15B_DORM_CONVERGENCE.流向类型, "同质收束", "convergence node should be typed");
assert.ok(nodes.ACT5_5A15B_DORM_CONVERGENCE.第六幕入口标签, "convergence node should write an act-six entry tag");
```

- [ ] **Step 4: Run failing test**

Run: `node playtest/5a-full-data.test.js`

Expected: FAIL because `v3` title, `ACT5_5A01C_LAKESIDE_SOFT_RETURN`, and `ACT5_5A15B_DORM_CONVERGENCE` do not exist yet.

### Task 2: Implement v3 Story Data

**Files:**
- Modify: `playtest/5a-full-data.js`
- Modify: `playtest/5a-full-playtest.html`

- [ ] **Step 1: Update chain**

Add these nodes to `chain`:

```js
"ACT5_5A01C_LAKESIDE_SOFT_RETURN",
"ACT5_5A01D_LAKESIDE_BACK",
"ACT5_5A15B_DORM_CONVERGENCE",
```

Place the lakeside nodes after `ACT5_5A01B_OUTSIDE_RETURN`; place convergence at the end of `chain` so its output choices cannot accidentally advance into `5X`.

- [ ] **Step 2: Update title**

Change the data title and HTML title to include:

```text
v3 回流外流分型系统
```

- [ ] **Step 3: Mark existing return/outflow nodes**

Add `流向类型: "同质回流"` to existing 5A return nodes:

```text
ACT5_5A01A_OUTSIDE_BENCH
ACT5_5A01B_OUTSIDE_RETURN
ACT5_5A10A_WASHROOM_RESET
ACT5_5A10B_WASHROOM_MESSAGE
ACT5_5A16A_STORE_WATER
ACT5_5A16B_STORE_RETURN
ACT5_5A19A_HALLWAY_REVIEW
ACT5_5A19B_HALLWAY_BACK
```

Add `流向类型: "异质外流"` to 5X nodes:

```text
ACT5_5X01_STAIRWELL_MUTE
ACT5_5X02_SOUTH_GATE_EDGE
ACT5_5X03_STRANGE_SEAT
ACT5_5X99_OUTFLOW_OUTPUT
```

- [ ] **Step 4: Add corridor heterogeneous soft return option**

Add a new option to `ACT5_5A01_CORRIDOR_RETURN`:

```js
opt("不进门，去湖边绕一圈", "...", { 回避值: 1, 晚风牵引: 1, 自我清晰: 1 }, ["ACT5_5A01C_LAKESIDE_SOFT_RETURN"], { 流向类型: "异质软回流" })
```

- [ ] **Step 5: Add lakeside two-beat soft return nodes**

Create `ACT5_5A01C_LAKESIDE_SOFT_RETURN` and `ACT5_5A01D_LAKESIDE_BACK`.

Requirements:

- Both nodes set `回环节点: true`.
- Both nodes set `流向类型: "异质软回流"`.
- Both nodes set `禁止解锁分线: true`.
- The first node includes `软串场角色: "晚风/湖边边缘同学"` and choices involving a WeChat or Douyin-style interruption.
- The second node returns to `ACT5_5A02_DOORWAY_PAUSE`.

- [ ] **Step 6: Add terminal homogeneous convergence**

Add an option to `ACT5_5A15_DOOR_SLAM_RISK`:

```js
opt("不再硬劝，把今晚收成低温版本", "...", { 低温共处: 2, 宿舍互信: 1, 回避值: -1 }, ["ACT5_5A15B_DORM_CONVERGENCE"], { 流向类型: "同质收束", 离场判定: "宿舍互信足够时进入5A小结局，否则同类离场动作会导向5X" })
```

Create `ACT5_5A15B_DORM_CONVERGENCE`.

Requirements:

- `流向类型: "同质收束"`
- `第六幕入口标签: "5A_低温稳定_有限修补"`
- `输出页: true`
- `分线: "5A_宿舍修补线_同质收束"`
- The prose must explain that this is not a new line and not a perfect repair.

### Task 3: Update Documentation and Planning Files

**Files:**
- Modify: `docs/5A节点验收表_v2.md`
- Modify: `findings.md`
- Modify: `progress.md`
- Modify: `task_plan.md`

- [ ] **Step 1: Update acceptance doc title and stats**

Change the heading to `# 5A 节点验收表 v3`.

Update stats to include node count and option count after tests report them.

Add rules:

```markdown
- 回流分为同质回流和异质软回流。
- 同质收束是 5A 内部小结局，不等同于 5X 外流。
- 异质软回流不得解锁 5B-5F 或 5X。
```

- [ ] **Step 2: Add planning log entries**

Append entries to `findings.md` and `progress.md` stating that v3 replaces v2.2's single-type loops with typed return/outflow.

### Task 4: Verify

**Files:**
- Test: `playtest/5a-full-data.test.js`
- Test: `playtest/app.smoke.test.js`
- Test: `playtest/engine.test.js`
- Check: `playtest/5a-full-data.js`
- Check: `playtest/app.js`
- Check: `沉浸式剧情互动系统_v2.json`

- [ ] **Step 1: Run data test**

Run: `node playtest/5a-full-data.test.js`

Expected: `5A full data tests passed`

- [ ] **Step 2: Run smoke and engine tests**

Run: `node playtest/app.smoke.test.js`

Expected: `app smoke test passed`

Run: `node playtest/engine.test.js`

Expected: `engine tests passed`

- [ ] **Step 3: Run syntax and JSON checks**

Run:

```powershell
node --check playtest/5a-full-data.js
node --check playtest/app.js
node -e "JSON.parse(require('fs').readFileSync('沉浸式剧情互动系统_v2.json','utf8')); console.log('json ok')"
```

Expected: syntax checks exit 0 and JSON prints `json ok`.

## Self-Review

- Spec coverage: covers typed returns, heterogeneous soft returns, homogeneous convergence, true outflow, and tests.
- Placeholder scan: no TODO/TBD placeholders.
- Type consistency: uses `流向类型`, `禁止解锁分线`, `软串场角色`, `第六幕入口标签`, and existing `回环节点`/`回流节点`.
- Scope: limited to 5A full playtest data, tests, playtest title, and docs.
