# 5A 叙事跑线审计 v3.5 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use $superpower-subagents (recommended) or $superpower-executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking via update_plan.

**Goal:** 产出 5A 的叙事审计表，先固定事件、暗线、钩子和人物弧光，再进入文本精修。

**Architecture:** 读取当前 5A 节点链，按剧情阶段聚合审计，不直接修改试玩数据。审计表作为下一轮改稿蓝图。

**Tech Stack:** Markdown 文档、当前静态试玩数据、Superpower planning 文件、`chinese-novelist` 小说化规则。

---

### Task 1: Recover Current 5A Structure

**Files:**
- Read: `playtest/5a-full-data.js`
- Read: `findings.md`
- Read: `docs/superpowers/specs/2026-05-15-5A-invalid-dialogue-v3.4.2-design.md`

- [x] **Step 1: Extract node chain**

Run:

```powershell
node -e "const d=require('./playtest/5a-full-data.js'); for (const id of d.chain){const n=d.nodes[id]; console.log([id,n['标题'],n['场景'],(n['可选互动']||[]).length,n['流向类型']||'',n['关键冲突节点']?'关键冲突':'',n['回环节点']?'回环':''].join('\t'));}"
```

Expected: list all 73 current chain nodes with title, scene, option count and return/outflow markers.

### Task 2: Run Chinese-Novel Narrative Audit

**Files:**
- Create: `docs/5A叙事跑线审计_v3.5.md`

- [x] **Step 1: Define event spine**

Group 5A into entrance audit, doorway return, old debt, rule meeting, outflow risk, canteen trial, exam relapse and outputs.

- [x] **Step 2: Define hidden lines**

Track dorm trust, avoidance, rule credibility, Lu Chen boundary, Zhou Yu face-defense, Tang Xiao rule-defense, Lin Yizhou mediation habit.

- [x] **Step 3: Define hooks**

For each stage, specify what question or object pulls the player forward.

- [x] **Step 4: Define drift guardrails**

Mark which support characters can appear, what they can affect, and which route they must not unlock.

### Task 3: Prepare Next Rewrite Scope

**Files:**
- Create: `docs/5A叙事跑线审计_v3.5.md`

- [x] **Step 1: List concrete rewrite targets**

For each stage, state what should be strengthened in the next data edit without changing structure.

- [x] **Step 2: Verify audit file exists**

Run:

```powershell
Test-Path 'docs\5A叙事跑线审计_v3.5.md'
```

Expected: `True`.
