# 5A 重复模板旁白纠偏 v3.4.1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use $superpower-subagents (recommended) or $superpower-executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking via update_plan.

**Goal:** Remove the repeated player-facing feedback template while preserving feedback density and 5A structure.

**Architecture:** Keep `状态影响` as the data-level state record. Replace the fixed natural-language state explanation in `enrichFeedback()` with varied diegetic afterglow text selected from character and object anchors.

**Tech Stack:** Plain JavaScript data module, Node assertion tests, markdown planning files.

---

### Task 1: Lock The Repetition With A Failing Test

**Files:**
- Modify: `playtest/5a-full-data.test.js`

- [x] **Step 1: Add banned player-facing patterns**

Add these patterns to the existing player-facing text ban list:

```js
/这一步会被记进/,
/同一张桌子、同一扇门、同一条群消息/,
/语气会悄悄变重或变轻/,
```

- [x] **Step 2: Run the test and verify it fails**

Run:

```bash
node playtest/5a-full-data.test.js
```

Expected: fail with `player-facing text should not contain meta phrase /这一步会被记进/`.

### Task 2: Replace The Fixed Template

**Files:**
- Modify: `playtest/5a-full-data.js`

- [x] **Step 1: Add a diegetic afterglow helper**

Create `feedbackAfterglow(text, effect)` with branches for 周屿、唐骁、陆沉、晚风、夏知微、许澈 and a small generic object-based fallback pool.

- [x] **Step 2: Use it from `enrichFeedback()`**

Keep the `raw.length >= 80` early return. For short feedback, append `feedbackAfterglow(text, effect)` instead of a fixed variable explanation.

- [x] **Step 3: Run focused verification**

Run:

```bash
node playtest/5a-full-data.test.js
node --check playtest/5a-full-data.js
```

Expected: both pass.

### Task 3: Record And Verify

**Files:**
- Modify: `task_plan.md`
- Modify: `findings.md`
- Modify: `progress.md`

- [x] **Step 1: Record root cause and correction**

Log that the repeated text came from automatic feedback enrichment, not a single node.

- [ ] **Step 2: Run fresh verification**

Run:

```bash
node playtest/5a-full-data.test.js
node playtest/app.smoke.test.js
node playtest/engine.test.js
node --check playtest/5a-full-data.js
node --check playtest/app.js
node --check playtest/5a-data.js
```

Expected: all pass.
