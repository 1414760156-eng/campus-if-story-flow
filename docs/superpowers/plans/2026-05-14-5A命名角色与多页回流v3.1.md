# 5A命名角色与多页回流 v3.1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use $superpower-subagents (recommended) or $superpower-executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking via update_plan.

**Goal:** 把 5A 回流段升级为命名人物、至少双回流方式、多页讨论和多次微抉择的可玩结构，同时保留唯一 5X 异质外流。

**Architecture:** 继续使用静态 `5a-full-data.js` 数据驱动试玩器，不新增运行时依赖。测试通过 `5a-full-data.test.js` 校验结构、命名、回流路径、唯一外流和默认通关。

**Tech Stack:** JavaScript CommonJS 数据文件、Node assert 测试、静态 HTML 试玩器。

---

### Task 1: 写 v3.1 失败测试

**Files:**
- Modify: `playtest/5a-full-data.test.js`

- [ ] **Step 1: 增加标题与密度断言**

```js
assert.ok(data.title.includes("v3.1"), "title should mark named multi-page return v3.1");
assert.ok(chain.length >= 50, `expected at least 50 nodes after v3.1 multi-page returns, got ${chain.length}`);
assert.ok(allOptions.length >= 126, `expected at least 126 options after v3.1 return expansion, got ${allOptions.length}`);
assert.ok(loopNodes.length >= 24, `expected at least 24 loop nodes after v3.1 return expansion, got ${loopNodes.length}`);
```

- [ ] **Step 2: 增加回流源点方式数量测试**

```js
function countReturnWays(sourceNodeId) {
  const source = nodes[sourceNodeId];
  assert.ok(source, `${sourceNodeId} should exist`);
  return (source.可选互动 || []).filter((option) =>
    (option.解锁 || []).some((target) => nodes[target] && nodes[target].回环节点)
  ).length;
}

[
  ["ACT5_5A01_CORRIDOR_RETURN", 3],
  ["ACT5_5A10_BEFORE_MEETING", 2],
  ["ACT5_5A16_CANTEEN_SEAT", 2],
  ["ACT5_5A19_EXAM_NIGHT_RETURN", 2],
].forEach(([sourceNodeId, minimum]) => {
  assert.ok(countReturnWays(sourceNodeId) >= minimum, `${sourceNodeId} should have ${minimum}+ return ways`);
});
```

- [ ] **Step 3: 增加多页回流路线测试**

```js
function assertMultiPageLoopRoute({ fromNodeId, optionPattern, routeNodeIds, returnNodeId }) {
  const fromNode = nodes[fromNodeId];
  assert.ok(fromNode, `${fromNodeId} should exist`);
  assert.ok(routeNodeIds.length >= 3, `${fromNodeId} route ${optionPattern} should define 3+ pages`);
  const optionIndex = (fromNode.可选互动 || []).findIndex((option) => optionPattern.test(option.选项));
  assert.ok(optionIndex >= 0, `${fromNodeId} should have loop option ${optionPattern}`);

  let loopGame = Object.assign(engine.createGame(data), { currentNodeId: fromNodeId });
  loopGame = engine.chooseOption(loopGame, optionIndex);
  routeNodeIds.forEach((nodeId, index) => {
    assert.strictEqual(loopGame.currentNodeId, nodeId, `${fromNodeId} route should visit ${nodeId}`);
    assert.strictEqual(nodes[nodeId].回环节点, true, `${nodeId} should be a loop node`);
    assert.strictEqual(nodes[nodeId].回流节点, returnNodeId, `${nodeId} should return to ${returnNodeId}`);
    if (index < routeNodeIds.length - 1) {
      loopGame = engine.chooseOption(loopGame, 0);
    }
  });
  loopGame = engine.chooseOption(loopGame, 0);
  assert.strictEqual(loopGame.currentNodeId, returnNodeId, `${routeNodeIds.at(-1)} should return to ${returnNodeId}`);
}
```

- [ ] **Step 4: 增加命名角色测试**

```js
const priorNamedCameos = new Set(["晚风", "夏知微", "沈嘉禾", "秦越", "陈老师", "周屿", "唐骁", "陆沉"]);
heterogeneousSoftReturnNodes.forEach((node) => {
  assert.ok(node.软串场角色, `${node.节点ID} should name soft cameo roles`);
  node.软串场角色.split("/").forEach((name) => {
    assert.ok(priorNamedCameos.has(name), `${node.节点ID} soft cameo ${name} should be a prior named character`);
  });
});

const nonOutflowText = allNodes
  .filter((node) => !node.外流线节点)
  .map((node) => JSON.stringify(node))
  .join("\n");
assert.ok(!/边缘同学|影像社同学|新认识的人|那个人|某个同学/.test(nonOutflowText), "non-outflow text should not use unnamed interactive characters");
```

- [ ] **Step 5: 增加 5X 许澈测试**

```js
const outflowLineText = outflowLineNodes.map((node) => JSON.stringify(node)).join("\n");
assert.ok(/许澈/.test(outflowLineText), "outflow line should name Xu Che");
assert.ok(!/新认识的人|那个人|刚在便利店门口见过的同学|那个新人的头像/.test(outflowLineText), "outflow line should not use unnamed newcomer phrasing");
["ACT5_5X02_SOUTH_GATE_EDGE", "ACT5_5X03_STRANGE_SEAT", "ACT5_5X99_OUTFLOW_OUTPUT"].forEach((nodeId) => {
  assert.strictEqual(nodes[nodeId].外流线新人物, "许澈", `${nodeId} should tag Xu Che as the outflow-line new character`);
});
```

- [ ] **Step 6: 运行测试确认红灯**

Run: `node playtest/5a-full-data.test.js`

Expected: FAIL，至少因为标题没有 v3.1 或回流方式/节点不存在。

### Task 2: 实现 v3.1 数据

**Files:**
- Modify: `playtest/5a-full-data.js`
- Modify: `playtest/5a-full-playtest.html`

- [ ] **Step 1: 更新标题**

把数据标题和 HTML title 更新为：

```text
第五幕 5A 宿舍修补线正式可玩版 v2.1-v2.2-v3.1 命名角色与多页回流系统
```

- [ ] **Step 2: 修正命名**

将非外流文本里的“影像社边缘同学/影像社同学/边缘同学”改为“夏知微”，并把 `软串场角色` 改为 `晚风/夏知微`。

将 5X 里的新人统一改为许澈，并在 5X02、5X03、5X99 加入：

```js
外流线新人物: "许澈"
```

- [ ] **Step 3: 扩展既有回流路线到三页**

插入以下节点并调整 chain：

```text
ACT5_5A01A2_OUTSIDE_TALK
ACT5_5A01C2_LAKESIDE_XIA
ACT5_5A10A2_WASHROOM_EDGE
ACT5_5A16A2_STORE_TALK
ACT5_5A19A2_HALLWAY_EDGE
```

同时把原第二页节点显式 `解锁` 到对应回流节点，防止 chain 插入后串线。

- [ ] **Step 4: 增加新回流方式**

新增以下三页路线：

```text
ACT5_5A01E_LAUNDRY_PAUSE -> ACT5_5A01F_LAUNDRY_LUCHEN -> ACT5_5A01G_LAUNDRY_BACK -> ACT5_5A02_DOORWAY_PAUSE
ACT5_5A10C_STAIRWELL_ZHOUYU -> ACT5_5A10D_STAIRWELL_TEXT -> ACT5_5A10E_STAIRWELL_BACK -> ACT5_5A11_TABLE_TIMER
ACT5_5A16C_SIDE_DOOR_XIA -> ACT5_5A16D_SIDE_DOOR_CAMERA -> ACT5_5A16E_SIDE_DOOR_BACK -> ACT5_5A17_AFTER_MEAL_CHAT
ACT5_5A19C_WATER_ROOM_THERMOS -> ACT5_5A19D_WATER_ROOM_LUCHEN -> ACT5_5A19E_WATER_ROOM_BACK -> ACT5_5A20_RELAPSE_CHOICE
```

每个新节点 2 个选项，效果只影响心态、人物关系或软资源，不新增外流点。

- [ ] **Step 5: 运行数据测试确认绿灯**

Run: `node playtest/5a-full-data.test.js`

Expected: PASS，输出 `5A full data tests passed`。

### Task 3: 同步文档与验收表

**Files:**
- Modify: `1-6幕剧情优化稿.md`
- Modify: `沉浸式剧情互动系统_v2.json`
- Modify: `docs/5A节点验收表_v2.md`
- Modify: `task_plan.md`
- Modify: `findings.md`
- Modify: `progress.md`

- [ ] **Step 1: 剧情优化稿同步**

加入 v3.1 规则：非 5X 人物必须命名且前文出现；每个回流源点至少两种回流方式；回流段至少三页并多次微抉择。

- [ ] **Step 2: JSON 系统同步**

在互动系统 JSON 的 5A 回流规则中加入 v3.1 字段，确保 JSON 仍可解析。

- [ ] **Step 3: 验收表同步**

把节点数量、选项数量、回环节点数量和新节点列表同步到验收表。

- [ ] **Step 4: planning-with-files 同步**

在 `task_plan.md`、`findings.md`、`progress.md` 记录本轮设计、红绿测试、环境限制和最终验证。

### Task 4: 完整验证

**Files:**
- Test: `playtest/5a-full-data.test.js`
- Test: `playtest/app.smoke.test.js`
- Test: `playtest/engine.test.js`
- Check: `playtest/5a-full-data.js`
- Check: `playtest/app.js`
- Check: `playtest/5a-data.js`
- Check: `沉浸式剧情互动系统_v2.json`
- Check: `interactive_system_v2.json`

- [ ] **Step 1: 跑完整命令**

```powershell
node playtest/5a-full-data.test.js
node playtest/app.smoke.test.js
node playtest/engine.test.js
node --check playtest/5a-full-data.js
node --check playtest/app.js
node --check playtest/5a-data.js
node -e "JSON.parse(require('fs').readFileSync('沉浸式剧情互动系统_v2.json','utf8')); console.log('json ok')"
node -e "JSON.parse(require('fs').readFileSync('interactive_system_v2.json','utf8')); console.log('legacy json ok')"
```

- [ ] **Step 2: 记录验证结果**

把命令、输出和任何限制写入 `progress.md`。

## Verification

完成标准：

- v3.1 测试先红后绿。
- 所有 5A 回流源点满足至少双回流方式，5A01 满足至少三种。
- 所有登记回流路线至少三页。
- 非 5X 无无名互动人物。
- 5X 新人统一命名为许澈。
- 唯一异质外流点仍只有一个。
- 静态试玩器仍可打开 `playtest/5a-full-playtest.html`。

## Next Skill

`$superpower-tdd` for implementation, then `$superpower-verification` before completion.
