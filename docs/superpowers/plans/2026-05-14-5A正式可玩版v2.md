# 5A 正式可玩版 v2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use $superpower-executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking via update_plan.

**Goal:** 将 5A 完整试玩版升级为正式可玩版 v2，增加多个回流点和关键节点选项密度，同时保持唯一外流点与主线完整。

**Architecture:** 继续使用现有静态 HTML、`5a-full-data.js` 和 `engine.js`。回流点通过 `选项.解锁` 进入短支线节点，短支线按 `chain` 顺序或显式解锁回主轴。外流点保持唯一，不新增外流入口。

**Tech Stack:** Plain JavaScript data module, Node.js tests, static HTML playtest.

---

### Task 1: Add Failing v2 Playability Tests

**Files:**
- Modify: `playtest/5a-full-data.test.js`

- [ ] **Step 1: Add v2 title and route-loop checks**

Add tests for:

- `data.title` contains `正式可玩版 v2`
- at least 4 loop nodes
- expected loop map:

```js
const expectedLoopReturns = {
  ACT5_5A01A_OUTSIDE_BENCH: "ACT5_5A02_DOORWAY_PAUSE",
  ACT5_5A10A_WASHROOM_RESET: "ACT5_5A11_TABLE_TIMER",
  ACT5_5A16A_STORE_WATER: "ACT5_5A17_AFTER_MEAL_CHAT",
  ACT5_5A19A_HALLWAY_REVIEW: "ACT5_5A20_RELAPSE_CHOICE",
};
```

- [ ] **Step 2: Add key-node option density checks**

Check:

```js
assert.ok(nodes.ACT5_5A11_TABLE_TIMER.可选互动.length >= 3);
assert.ok(nodes.ACT5_5A15_DOOR_SLAM_RISK.可选互动.length >= 4);
assert.ok(nodes.ACT5_5A20_RELAPSE_CHOICE.可选互动.length >= 3);
assert.strictEqual(outflowOptions.length, 1);
```

- [ ] **Step 3: Add branch traversal checks**

For each new loop, start a game at the predecessor node, choose the loop option by text, assert it enters the loop, then choose option 0 and assert it returns to the correct node.

- [ ] **Step 4: Run and confirm RED**

Run:

```powershell
node playtest/5a-full-data.test.js
```

Expected: fails because v2 loop nodes do not exist yet.

### Task 2: Implement v2 Loop Nodes and Options

**Files:**
- Modify: `playtest/5a-full-data.js`
- Modify: `playtest/5a-full-playtest.html`

- [ ] **Step 1: Update title**

Set data title to `第五幕 5A 宿舍修补线正式可玩版 v2`.

- [ ] **Step 2: Insert loop nodes in chain**

Add:

```js
"ACT5_5A10A_WASHROOM_RESET",
"ACT5_5A16A_STORE_WATER",
"ACT5_5A19A_HALLWAY_REVIEW",
```

at the appropriate chain positions.

- [ ] **Step 3: Add route options**

Update:

- `ACT5_5A10_BEFORE_MEETING`: add `先去水房洗把脸，再回来开口` -> `ACT5_5A10A_WASHROOM_RESET`
- `ACT5_5A16_CANTEEN_SEAT`: add `不进食堂，先去便利店买水` -> `ACT5_5A16A_STORE_WATER`
- `ACT5_5A19_EXAM_NIGHT_RETURN`: add `去走廊背两页，再回来处理` -> `ACT5_5A19A_HALLWAY_REVIEW`

Existing normal options on those predecessor nodes should explicitly unlock the correct next main node.

- [ ] **Step 4: Add loop node content**

Each new loop node gets:

- `回环节点: true`
- `回流节点`
- `低操作高沉浸段: true`
- 2 options with 80+ char feedback

### Task 3: Expand Key Node Choices

**Files:**
- Modify: `playtest/5a-full-data.js`

- [ ] **Step 1: Expand `ACT5_5A11_TABLE_TIMER`**

Add option:

```js
opt("先让每个人写一句不能再忍的底线", "...", { 事实清单: 1, 规则可信度: 3, 陆沉表达度: 1 })
```

- [ ] **Step 2: Expand `ACT5_5A20_RELAPSE_CHOICE`**

Add option:

```js
opt("先低声问陆沉还能不能撑一下", "...", { 陆沉信任: 2, 宿舍互信: 1, 自我清晰: 1 })
```

### Task 4: Sync Docs and Verify

**Files:**
- Modify: `1-6幕剧情优化稿.md`
- Modify: `沉浸式剧情互动系统_v2.json`
- Modify: `task_plan.md`
- Modify: `findings.md`
- Modify: `progress.md`
- Create: `docs/5A节点验收表_v2.md`

- [ ] **Step 1: Record v2 rule**

Document:

- 5A v2 now uses multiple回流点.
- only one外流点 remains.
- key nodes should carry 3-5 options.

- [ ] **Step 2: Create node audit table**

Create `docs/5A节点验收表_v2.md` listing each node with categories:

- 主轴
- 心理选择
- 路线级选择
- 回环节点
- 强冲突点
- 唯一外流点
- 外流定轨节点
- 输出页

- [ ] **Step 3: Run verification**

Run:

```powershell
node playtest/5a-full-data.test.js
node playtest/app.smoke.test.js
node playtest/engine.test.js
node --check playtest/5a-full-data.js
node --check playtest/app.js
node -e "const fs=require('fs'); JSON.parse(fs.readFileSync('沉浸式剧情互动系统_v2.json','utf8')); console.log('json ok');"
```

Expected all pass.

## Verification

v2 is complete only if:

- data title marks v2,
- loop nodes >= 4,
- outflow options == 1,
- key nodes meet option density,
- normal traversal reaches 5A output,
- outflow traversal reaches 5X output,
- JSON parses.

## Next skill

`$superpower-executing-plans`
