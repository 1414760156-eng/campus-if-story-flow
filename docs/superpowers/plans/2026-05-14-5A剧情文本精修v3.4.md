# 5A 剧情文本精修 v3.4 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use $superpower-subagents (recommended) or $superpower-executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking via update_plan.

**Goal:** Polish 5A's existing narrative text and player choices without expanding the route structure.

**Architecture:** Keep `playtest/5a-full-data.js` as the single data source. Add tests in `playtest/5a-full-data.test.js` that guard against system-like player-facing copy and verify the polished key nodes remain structurally valid.

**Tech Stack:** Static JavaScript data, Node `assert` tests, markdown/JSON documentation.

---

### Task 1: Add v3.4 Text-Quality Tests

**Files:**
- Modify: `playtest/5a-full-data.test.js`

- [ ] **Step 1: Write failing tests**

Add assertions that:
- `data.title.includes("v3.4")`.
- Player-facing option text and feedback do not include meta phrases such as `系统写入`, `导出本次`, `确认本线结果`, `查看被远离的关系清单`, `确认5A同质收束`.
- Output nodes have at least two diegetic options whose text does not begin with `确认` or `查看`.
- `ACT5_5A11_TABLE_TIMER`, `ACT5_5A15_DOOR_SLAM_RISK`, and `ACT5_5A20_RELAPSE_CHOICE` still mention 周屿、唐骁、陆沉 and concrete objects.

- [ ] **Step 2: Run the failing test**

Run: `node playtest/5a-full-data.test.js`

Expected: fails on missing `v3.4` or meta copy.

### Task 2: Polish Data Text

**Files:**
- Modify: `playtest/5a-full-data.js`
- Modify: `playtest/5a-full-playtest.html`

- [ ] **Step 1: Update title**

Set title to include `v3.4 剧情文本精修`.

- [ ] **Step 2: Rewrite router and output nodes**

Replace player-visible system text with diegetic actions:
- Router becomes手机消息盘点, not “系统分线整理”.
- 5A normal output becomes bedtime/毕业活动前的关系留痕.
- 5A low-temperature convergence becomes门边停住/规则纸留在墙边.
- 5X output becomes便利店窗边/不回头的低温确认.

- [ ] **Step 3: Polish conflict nodes**

Improve:
- `ACT5_5A11_TABLE_TIMER`
- `ACT5_5A12_OLD_DEBT_NAMING`
- `ACT5_5A13_SIDE_OR_SELF`
- `ACT5_5A15_DOOR_SLAM_RISK`
- `ACT5_5A20_RELAPSE_CHOICE`

Keep node IDs and unlock targets unchanged.

- [ ] **Step 4: Run test**

Run: `node playtest/5a-full-data.test.js`

Expected: `5A full data tests passed`.

### Task 3: Sync Docs and Verify

**Files:**
- Modify: `docs/5A节点验收表_v2.md`
- Modify: `1-6幕剧情优化稿.md`
- Modify: `沉浸式剧情互动系统_v2.json`
- Modify: `task_plan.md`
- Modify: `findings.md`
- Modify: `progress.md`

- [ ] **Step 1: Update docs with v3.4**

Record that v3.4 is a text polish pass, not a structure expansion.

- [ ] **Step 2: Run verification**

Run:
- `node playtest/5a-full-data.test.js`
- `node playtest/app.smoke.test.js`
- `node playtest/engine.test.js`
- `node --check playtest/5a-full-data.js`
- `node --check playtest/app.js`
- `node --check playtest/5a-data.js`
- JSON parse checks for `沉浸式剧情互动系统_v2.json` and `interactive_system_v2.json`

Expected: all pass.
