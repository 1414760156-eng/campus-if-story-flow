# 5A 唯一外流点 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use $superpower-executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking via update_plan.

**Goal:** 在 5A 完整试玩版中加入且只加入一个外流点，使玩家能在强冲突处高代价离开修补主轴，并进入新的宿命式 `5X_离场外流线`。

**Architecture:** 继续使用现有 `解锁` 字段实现从 5A15 进入外流线。外流线节点插入 `chain` 中，但普通路线通过显式解锁跳过它们；只有唯一外流选项能进入 `5X_离场外流线`。外流线内部是定轨结构，像第七幕一样方向不可改变，选项只影响人物关系、心理状态、记忆标签和终章回响。

**Tech Stack:** Plain JavaScript data module, Node.js tests, static HTML playtest.

---

### Task 1: Add Failing Unique-Outflow Test

**Files:**
- Modify: `playtest/5a-full-data.test.js`

- [ ] **Step 1: Add assertions**

Add checks after the existing loop-node branch assertions:

```js
const allNodes = Object.values(nodes);
const outflowLineNodes = allNodes.filter((node) => node.外流线节点);
const outflowOptions = allNodes.flatMap((node) =>
  (node.可选互动 || [])
    .filter((option) => option.外流点)
    .map((option) => ({ node, option }))
);
assert.strictEqual(outflowOptions.length, 1, "5A should have exactly one outflow option");
assert.strictEqual(outflowOptions[0].node.节点ID, "ACT5_5A15_DOOR_SLAM_RISK", "the only outflow point should be at door slam risk");
assert.deepStrictEqual(outflowOptions[0].option.解锁, ["ACT5_5X01_STAIRWELL_MUTE"], "outflow option should unlock new outflow line");
assert.deepStrictEqual(
  outflowLineNodes.map((node) => node.节点ID),
  ["ACT5_5X01_STAIRWELL_MUTE", "ACT5_5X02_SOUTH_GATE_EDGE", "ACT5_5X03_STRANGE_SEAT", "ACT5_5X99_OUTFLOW_OUTPUT"],
  "outflow line should be a new stable short line"
);
outflowLineNodes.forEach((node) => {
  assert.strictEqual(node.分线, "5X_离场外流线", `${node.节点ID} should belong to the new outflow line`);
  assert.strictEqual(node.定轨节点, true, `${node.节点ID} should be deterministic`);
  assert.strictEqual(node.回流节点, undefined, `${node.节点ID} should not return to old lines`);
});
const forbiddenRouteKeys = ["解锁5A", "解锁5B", "解锁5C", "解锁5D", "解锁5E", "解锁5F"];
outflowLineNodes.forEach((node) => {
  (node.可选互动 || []).forEach((option) => {
    assert.ok(!forbiddenRouteKeys.some((key) => Object.prototype.hasOwnProperty.call(option, key)), `${node.节点ID}/${option.选项} should not unlock old route keys`);
  });
});
```

- [ ] **Step 2: Add route checks**

Use `engine.createGame(data)` and set `currentNodeId` to `ACT5_5A15_DOOR_SLAM_RISK` for route checks:

```js
let normalDoorGame = Object.assign(engine.createGame(data), { currentNodeId: "ACT5_5A15_DOOR_SLAM_RISK" });
normalDoorGame = engine.chooseOption(normalDoorGame, 0);
assert.strictEqual(normalDoorGame.currentNodeId, "ACT5_5A16_CANTEEN_SEAT", "normal door choice should skip outflow");

let outflowGame = Object.assign(engine.createGame(data), { currentNodeId: "ACT5_5A15_DOOR_SLAM_RISK" });
const outflowIndex = nodes.ACT5_5A15_DOOR_SLAM_RISK.可选互动.findIndex((option) => option.外流点);
outflowGame = engine.chooseOption(outflowGame, outflowIndex);
assert.strictEqual(outflowGame.currentNodeId, "ACT5_5X01_STAIRWELL_MUTE", "outflow option should move to the new outflow line");
outflowGame = engine.chooseOption(outflowGame, 0);
assert.strictEqual(outflowGame.currentNodeId, "ACT5_5X02_SOUTH_GATE_EDGE", "outflow line should continue away from the dorm");
outflowGame = engine.chooseOption(outflowGame, 0);
assert.strictEqual(outflowGame.currentNodeId, "ACT5_5X03_STRANGE_SEAT", "outflow line should reach a new scene");
outflowGame = engine.chooseOption(outflowGame, 0);
assert.strictEqual(outflowGame.currentNodeId, "ACT5_5X99_OUTFLOW_OUTPUT", "outflow line should end at its own output page");
```

- [ ] **Step 3: Run and confirm RED**

Run:

```powershell
node playtest/5a-full-data.test.js
```

Expected: fail because the new outflow line and outflow option do not exist yet.

### Task 2: Implement Unique Outflow Line

**Files:**
- Modify: `playtest/5a-full-data.js`

- [ ] **Step 1: Insert outflow line in chain**

Insert immediately after `ACT5_5A15_DOOR_SLAM_RISK`:

```js
"ACT5_5X01_STAIRWELL_MUTE",
"ACT5_5X02_SOUTH_GATE_EDGE",
"ACT5_5X03_STRANGE_SEAT",
"ACT5_5X99_OUTFLOW_OUTPUT",
```

- [ ] **Step 2: Update `ACT5_5A15_DOOR_SLAM_RISK`**

Set the existing three options to unlock `ACT5_5A16_CANTEEN_SEAT`.

Add one outflow option:

```js
opt("不再劝了，推门出去把这场谈话留在身后", "...", { 宿舍线外流: 1, 低温共处: 2, 回避值: 4, 宿舍互信: -6, 晚风牵引: 1 }, ["ACT5_5A15X_STAIRWELL_OUTFLOW"], { 外流点: true })
```

- [ ] **Step 3: Add outflow line nodes**

Create a deterministic outflow line:

- `ACT5_5X01_STAIRWELL_MUTE`: 楼梯间静音，玩家把宿舍群、晚风、家里消息全部按掉。
- `ACT5_5X02_SOUTH_GATE_EDGE`: 南门外沿，原来的五神院动线退后，玩家进入不熟悉的街边。
- `ACT5_5X03_STRANGE_SEAT`: 陌生座位，可以是便利店窗边、夜车站台或临时活动外场。
- `ACT5_5X99_OUTFLOW_OUTPUT`: 外流输出页，写入低温外流标签。

Each node should include:

- `分线: "5X_离场外流线"`
- `外流线节点: true`
- `定轨节点: true`
- `低操作高沉浸段: true`

`ACT5_5X99_OUTFLOW_OUTPUT` should include `输出页: true`.

Internal option design:

- Do not use `解锁` on internal outflow-line options unless pointing to the next fixed outflow node.
- Prefer normal chain progression.
- Effects should only change psychology, relationship temperature, memory tags, or final echo, for example `自我麻木`、`关系降温`、`宿舍线外流`、`晚风远距牵引`、`家庭消息未读`、`终章疏离回响`.

- [ ] **Step 5: Make 5A outflow ending source-specific**

Set `ACT5_5X99_OUTFLOW_OUTPUT` extra fields:

```js
{
  外流来源线: "5A_宿舍修补线",
  外流结局类型: "新人逢场作戏_宿舍渐远_遗憾回响"
}
```

The output setup must state:

- 林亦舟认识新的人，起初觉得还不错。
- 宿舍渐行渐远。
- 自己被一切拉远，找不清方向。
- 新人关系最后显得像逢场作戏。
- 结尾留下遗憾，而不是爽快新开始。

- [ ] **Step 6: Lock later acts to the outflow line**

Set `ACT5_5X99_OUTFLOW_OUTPUT` extra fields:

```js
{
  跨幕延续: "第六第七幕仅走外流单线",
  软互动规则: "可同场景经过和过场互动，不开放回流抉择"
}
```

Record in narrative docs:

- 第六幕和第七幕仍走外流单线。
- 可以经过宿舍、食堂、毕业活动、家中饭桌等同样场景。
- 可以和周屿、唐骁、陆沉、晚风、父母等角色发生软互动。
- 软互动只存在于过场剧情，不涉及抉择。
- 不允许从第六或第七幕回到宿舍修补线、晚风线、家庭线、学业线、作品线或资源线。

- [ ] **Step 4: Prevent normal path from entering the outflow line**

Since the outflow line is in `chain`, confirm all ordinary 5A15 options unlock `ACT5_5A16_CANTEEN_SEAT` and never enter `ACT5_5X01_STAIRWELL_MUTE`.

### Task 3: Sync Narrative Rules and Verify

**Files:**
- Modify: `1-6幕剧情优化稿.md`
- Modify: `沉浸式剧情互动系统_v2.json`
- Modify: `task_plan.md`
- Modify: `findings.md`
- Modify: `progress.md`

- [ ] **Step 1: Record rule**

Document “多个回流点，一个外流点；外流点进入宿命式新线，不回原多线；外流线内部选择只改关系和心理” in the fifth-act playability rules.

- [ ] **Step 2: Run verification**

Run:

```powershell
node playtest/5a-full-data.test.js
node playtest/app.smoke.test.js
node playtest/engine.test.js
node --check playtest/5a-full-data.js
node --check playtest/app.js
node -e "const fs=require('fs'); JSON.parse(fs.readFileSync('沉浸式剧情互动系统_v2.json','utf8')); console.log('json ok');"
```

Expected all pass.

## Verification

The feature is complete only when tests prove:

- exactly one outflow option exists,
- exactly one outflow option exists,
- one short outflow line exists,
- outflow line nodes are deterministic,
- outflow output is judged by `5A_宿舍修补线`,
- outflow output uses `新人逢场作戏_宿舍渐远_遗憾回响`,
- later acts are locked to the outflow line with soft interactions only,
- the outflow point is at `ACT5_5A15_DOOR_SLAM_RISK`,
- ordinary options skip the outflow node,
- the outflow line goes to its own output,
- normal full traversal still reaches 5A output without entering outflow.

## Next skill

`$superpower-executing-plans`
