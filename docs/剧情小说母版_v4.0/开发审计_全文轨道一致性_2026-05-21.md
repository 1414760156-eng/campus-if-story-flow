# 全文轨道一致性审计 2026-05-21

## 审计结论

本次按“重开对话后从本地文件重新接管”的标准审计 `docs/剧情小说母版_v4.0`，不依赖旧对话记忆。

结论：当前母版和 IF 开发层没有发现会导致下一步开发偏离轨道的硬漂移。可以继续顺着 `ACT7-E06-THESIS-REVISION` 往下拆 `ACT7-E07-DEFENSE`。

本审计是“全文轨道审计”：覆盖全目录文件盘点、全文关键词扫描、JSON 校验、路线 / 事件覆盖统计，以及关键总控文件人工复核。它不是出版级逐字润色校对。

## Git 基线

- 仓库：`C:\Users\qingguang\Documents\Codex\2026-05-14\1-6-npc-json-1-6`
- 分支：`codex/history-snapshot`
- 审计前工作区：干净
- 最近提交：
  - `ad55e97 Add Act7 thesis revision development pages`
  - `1ff74b7 Capture IF longline and Act7 development snapshot`
  - `43db0a8 Constrain IF romance outflow permissions`

## 扫描范围

- 目录文件总数：58
- 文件类型：54 个 `.md`、2 个 `.json`、1 个 `.html`、1 个 `.txt`
- 目录总大小：7,258,724 bytes
- `count_v4.py` 当前统计：
  - `BODY_TOTAL 1113362`
  - `MD_TOTAL 1930623`
  - `CHAPTERS 238`

说明：计数脚本在当前 PowerShell 控制台中显示中文文件名乱码，但总数正常输出。

## 结构校验

两个 JSON 均已通过 `ConvertFrom-Json`：

- `开发数据_IF剧情页级JSON_DEFAULT-4XX_v1.json`
- `开发样板_IF剧情页级JSON拆分样板_v1.json`

当前正式 JSON 层仍只有 `DEFAULT-4XX` 第一版，这是预期进度，不是漂移。其它路线处于长线稿 / 细稿层，尚未批量拆正式 JSON。

## 核心轨道

### 总控锁点

`00_总控与写作规则.md` 当前仍明确以下硬约束：

- 小说母版优先作为小说成立，再服务互动拆分。
- 宿舍四人关系优先，周屿、林亦舟、唐骁、陆沉是主轴。
- 关键事件保持：入学、军训手机袋、汇演、班会、主持招新、第五幕收束、第六幕准结局、终章最后一个新年。
- 许澈只属于唯一 `5X` 硬外流；进入后第六幕和终章继续外流单线，不回原来的 `5A-5F`。
- 第七卷负责毕业回声，不强制和解，不突然煽情。
- 小说正文禁用游戏口吻：主线、副线、节点、flag、触发、权重、判定、回主线、资源线等。

### IF 路线池

`开发规则_IF命运线池化架构.md`、`开发索引_时期外流点路线表.md` 和 `开发索引_IF命运分歧开发矩阵.md` 当前保持一致：

- 默认宿舍主轴完整外流只保留宿舍站队线和晚风线。
- `R5-ROMANCE` 不能从主轴凭空跳出，必须来自 `A3-ACTIVITY-PUBLIC` 父池中有女生连续出现的方向。
- 沈嘉禾、夏知微是当前可承认的 A3 女生候选来源；许棠只作为公开叙事 / 授权边界软照面，不写入感情候选。
- `R5X-HARD` 是唯一硬外流，必须二次确认，锁定后设置 `route_lock = "5x"` 和 `hard_outflow = true`。

### 第五 / 第六卷玩法节奏

`开发规则_IF第五第六卷玩法节奏硬约束.md` 已接入总表、矩阵和时期外流表：

- 普通长线结算前必须满足第五卷最低主线选择窗口。
- 第六卷进入命运池后固定为 20 个剧情段。
- 每 4 段落 1 个选择窗口，共 5 个选择窗口。
- 每个选择窗口只能保留两个当前池内方向，不能重新打开其它完整路线。

11 个长线开发文件均含 `act6_20_scene_mapping`，未发现某条长线缺第六卷承接占位。

## ACT7 审计

`开发规则_IF第七卷毕业结算与共用事件变体矩阵.md` 当前固定 12 个共用事件点：

1. `ACT7-E01-PRE-REGISTER`
2. `ACT7-E02-PUBLIC-SCOPE`
3. `ACT7-E03-LAST-NEW-YEAR`
4. `ACT7-E04-THESIS-TOPIC`
5. `ACT7-E05-MARCH-QUEUE`
6. `ACT7-E06-THESIS-REVISION`
7. `ACT7-E07-DEFENSE`
8. `ACT7-E08-GRAD-PHOTO`
9. `ACT7-E09-DORM-CLEAR`
10. `ACT7-E10-LAST-MEAL`
11. `ACT7-E11-LEAVING-PAPERS`
12. `ACT7-E12-STATION-AFTER`

全文事件 ID 覆盖统计显示 12 个事件均有命中。当前重点文件中，`ACT7-E03` 与 `ACT7-E06` 已有剧情页级细稿；`ACT7-E07` 仍是下一步待拆细稿。

### E03 状态

`开发细稿_IF第七卷ACT7-E03最后一个新年剧情页.md` 已覆盖 11 个路线变体：

- `ACT7-DEFAULT`
- `ACT7-ZHOU`
- `ACT7-TANG`
- `ACT7-LUCHEN`
- `ACT7-LIEFLAT`
- `ACT7-WANFENG`
- `ACT7-ROMANCE`
- `ACT7-WORK`
- `ACT7-A3`
- `ACT7-PERFECT`
- `ACT7-R5X`

硬条件：`opening_contrast_seen = true`，否则 `ACT7-E03` 不算完成。

### E06 状态

`开发细稿_IF第七卷ACT7-E06改论文毕业设计剧情页.md` 已覆盖同一组 11 个路线变体，并明确：

- `event_id = ACT7-E06-THESIS-REVISION`
- `previous_event = ACT7-E05-MARCH-QUEUE`
- `next_event = ACT7-E07-DEFENSE`
- `route_switch_allowed = false`
- `same_mother_event = true`
- 每条路线必须出现导师 / 指导老师修改意见、版本、截止时间和二方向选择。
- 每条路线必须写出“帮看不等于替写”。
- `R5X-HARD` 只保留软照面、消息、同场流程或低温回声，不得回原多线。

硬条件：必须记录 `thesis_revision_comments_seen`、`thesis_revision_version`、`thesis_revision_helper`、`thesis_boundary_checked`、`revision_submitted_before_deadline`，且 `thesis_boundary_checked = true`。

## 风险词扫描结论

### 占位 / 未完成词

全文扫描命中 `待补`、`后续补充`、`占位` 等词，但抽查后未发现新开发文件存在实际 TODO 占位。

`ACT7-E06` 中两处 `后续补充` 都是剧情对象：

- 初表阶段“数据可写后续补充”。
- 陈老师红批划掉“后续补充”，要求补清数据来源和展示边界。

这两处是 E06 的核心现实压力，不是未完成标记。

### 漂移词

`大团圆`、`奖励线`、`回原多线`、`免费工具` 等词主要出现在禁止项、防漂移说明或风险表达中。

重点确认：

- `ACT7-E03` 和 `ACT7-E06` 中的 `不得回原多线` 是 5X 防线文本。
- `ACT7-E06` 中的 `免费工具` 用于限制沈嘉禾 / 夏知微不能变成改稿工具，不是把候选对象功能化。
- 未发现 E06 把“改论文”写成大家一起熬夜和好、跨线补偿、路线重开或学业流水账。

### 正文游戏口吻

对 `正文_*.md` 扫描游戏口吻禁词，未发现 `玩家`、`flag`、`好感度`、`回主线` 等硬违规词。

命中的 `选项` 均为表格、系统页面、考试题库或投票选项等现实语义；命中的 `系统提示` 为设备安全弹出提示，不是互动系统口吻。

## 当前可继续开发的方向

安全下一步：

1. 拆 `ACT7-E07-DEFENSE` 答辩 / 答辩后剧情页级细稿。
2. 直接读取 `ACT7-E06` 的提交版本、帮看关系和 `thesis_boundary_checked`。
3. 仍覆盖 11 个路线变体。
4. 继续保持 `route_switch_allowed = false`。
5. 答辩必须写谁在门口、谁不在、谁只发消息、谁赶流程、谁等到最后一分钟。
6. 完成 E07 后，再把 `ACT7-E03`、`ACT7-E06`、`ACT7-E07` 三个核心事件写入正式 JSON 第七卷结算层。

## 仍需注意的开发缺口

- `ACT7-E07-DEFENSE` 细稿尚未创建，是下一步。
- 第七卷 12 个事件尚未全部拆为正式 JSON。
- 正式 JSON 目前只有 `DEFAULT-4XX`，其它路线需后续按同一结构批量拆分。
- 本报告未做出版级错别字和逐句文学润色；若后续进入交付阅读版，应另开校对任务。

## 最终判断

当前开发轨道稳定。下一步拆 `ACT7-E07-DEFENSE` 不会和现有规则冲突，只要继续遵守：

- 第七卷只结算，不重新分流。
- E07 承接 E06 的版本、帮看和边界记录。
- 11 条路线同事件不同镜头。
- 5X 不回原多线。
- 改论文 / 答辩继续服务关系温度，而不是纯流程通关。
